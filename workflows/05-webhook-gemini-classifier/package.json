{
  "name": "webhook-gemini-classifier-tests",
  "version": "1.0.0",
  "description": "Test suite per il workflow n8n webhook-gemini-classifier",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "jest": "^29.7.0"
  }
}
